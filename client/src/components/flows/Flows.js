import React, { Component } from 'react';
import '../../styles/components/flows.scss';

const Flows = () => {
    return (
      <div className="flows">
        <h2>Sorry, we under construction yet .. </h2>
      </div>

    );
};

export default Flows;
